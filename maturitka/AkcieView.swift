import SwiftUI

struct AkcieView: View {
    var body: some View {
        VStack {
            Text("Novinky")
                .font(.title)
                .padding()
        }
        .navigationTitle("Akcie")
    }
}
