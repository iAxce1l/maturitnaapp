import SwiftUI

struct KalendarView: View {
    var body: some View {
        VStack {
            Text("Kalendar")
                .font(.title)
                .padding()
        }
        .navigationTitle("Kalendar")
    }
}
